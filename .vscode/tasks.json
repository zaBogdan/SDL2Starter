{
    "tasks": [
      {
        "type": "cppbuild",
        "label": "build",
        "command": "make",
        "args": [
        ],
        "options": {
          "cwd": "${workspaceFolder}",
          "env": {
            "LIBRARY_PATH": "$LIBRARY_PATH:/opt/homebrew/lib"
          }
        },
        "problemMatcher": [
          "$gcc"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "detail": "Builds our program in debug mode using the existing Makefile"
      },
      {
        "type": "cppbuild",
        "label": "build release",
        "command": "make",
        "args": [
          "release"
        ],
        "options": {
          "cwd": "${workspaceFolder}",
          "env": {
            "LIBRARY_PATH": "$LIBRARY_PATH:/opt/homebrew/lib"
          }
        },
        "problemMatcher": [
          "$gcc"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "detail": "Builds our program in release mode using the existing Makefile"
      }

    ],
    "version": "2.0.0"
  }